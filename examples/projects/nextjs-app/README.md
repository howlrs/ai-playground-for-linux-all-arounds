# Next.js App テンプレート

Next.js 15 + TypeScript の最小構成。

## セットアップ

```bash
cp -r ~/examples/projects/nextjs-app ~/workspace/my-app
cd ~/workspace/my-app
npm install
npm run dev
```

## AIエージェントへの依頼例

```
claude > "ダッシュボードページを追加して。Chart.jsでグラフ表示"
claude > "Prisma + SQLiteでユーザー管理APIを追加して"
claude > "認証をNextAuth.jsで実装して"
```
